server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/auth_service
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
springdoc:
  swagger-ui:
    path: /swagger-ui.html
jwt:
  secret: "clave_muy_segura_de_al_menos_32_bytes_de_largo"
  expiration: 86400000 # 24 horas en milisegundos
